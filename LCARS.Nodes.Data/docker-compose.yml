version: '3.4'

services:
  lcarsnodesdata:
    image: ryanchill/lcarsnodesdata:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 3000:3000
    volumes:
      - primaryadjunct:/usr/src/app/logs
